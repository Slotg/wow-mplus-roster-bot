[build]
# Dockerfile lives in apps/bot, but build context is always the repo root.
# All COPY paths in the Dockerfile are relative to the repo root.
dockerfilePath = "apps/bot/Dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
